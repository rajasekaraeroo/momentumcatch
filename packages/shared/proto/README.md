# Protobuf definitions

Download `MarketDataFeedV3.proto` from the Upstox developer documentation
(Market Data Feed V3 page) and place it in this folder before implementing
the feed handler. Decode with `protobufjs`. Do not hand-write the schema
from memory — the proto file is the contract.
